create table if not exists users
(
    id int not null,
    username           varchar not null,
    about            double precision not null,
    anonymous           double precision not null,
    name        double precision not null
    email
);


   ID   INT              NOT NULL,
   NAME VARCHAR (20)     NOT NULL,
   AGE  INT              NOT NULL,
   ADDRESS  CHAR (25),
   SALARY   DECIMAL (18, 2),
   PRIMARY KEY (ID)